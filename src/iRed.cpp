#include "iRed.h"
#include "Arduino.h"
#include <movement.h>

void wall_Align ()
{

    // If left IR sensor is closer, rotate anti-clockwise
    // If right IR sensor is closer, rotate clockwise
    // Align when both IR sensor readings are approximately identical

}